# Microsoft Store 发布填写手册（i thinking）

面向 Partner Center **EXE/MSI（Win32）** 提交。与包托管细节交叉引用：[`store-package-handoff.md`](./store-package-handoff.md)。

| 项 | 当前值 |
|----|--------|
| 产品名 | i thinking |
| 版本 | 1.3.0（随发版更新） |
| 发布者 / 开发者展示名 | i thinking / layen |
| 包形态 | Tauri NSIS `.exe`（非 MSIX） |
| 文档日期 | 2026-08-10 |

相关文稿：

| 文档 | 用途 |
|------|------|
| [`privacy-policy.zh-CN.md`](./privacy-policy.zh-CN.md) | 隐私策略（属性页粘贴或 URL） |
| [`license-terms.zh-CN.md`](./license-terms.zh-CN.md) | 适用的许可条款（一览页必填） |
| [`认证说明.md`](./认证说明.md) | 认证测试人员说明 |
| [`store-package-handoff.md`](./store-package-handoff.md) | 包 URL / R2 / Worker |

官方入口：[创建提交](https://learn.microsoft.com/zh-cn/windows/apps/publish/publish-your-app/msi/create-app-submission) · [属性](https://learn.microsoft.com/zh-cn/windows/apps/publish/publish-your-app/msi/enter-app-properties) · [包](https://learn.microsoft.com/zh-cn/windows/apps/publish/publish-your-app/msi/upload-app-packages) · [一览](https://learn.microsoft.com/zh-cn/windows/apps/publish/publish-your-app/msi/add-and-edit-store-listing-info)

---

## 0. 提交路径说明（先读）

Partner Center 有两条路，**不要混**：

| 路径 | 包怎么交 | 你们现状 |
|------|----------|----------|
| **EXE or MSI app**（`win32apps/...`） | 填 **HTTPS 包托管 URL** | ✅ 当前路径 |
| MSIX / 传统应用 | 直接上传 `.msix` | ❌ Tauri v2 不原生出 MSIX |

因此：**必须**自备 CDN/R2/Worker 托管安装包；看不到「上传 MSIX」是正常的。

上架硬性前提（与托管无关）：

1. 安装包为**离线完整** `.exe` / `.msi`（非 stub）
2. **代码签名**证书链到 Microsoft Trusted Root（自签名不行）
3. **静默安装**（NSIS：`/S`）
4. WebView2 使用 **`offlineInstaller`**（Store 要求离线安装包；当前 `tauri.conf.json` 已配置）
5. 包 URL **版本化**，提交后该 URL 上的文件**不得覆盖**

---

## 1. 总清单（按 Partner Center 顺序）

| 步骤 | 页面 | 必填要点 | 状态备忘 |
|------|------|----------|----------|
| 1 | 定价和可用性 | 市场、免费/付费、发布方式 | □ |
| 2 | 属性 | 类别、隐私、支持、产品声明 | □ |
| 3 | 年龄分级 | 问卷生成评级 | □ |
| 4 | 包 | HTTPS 包 URL、架构、静默参数 | □ |
| 5 | Store 一览 | 说明、截图、1:1 徽标、许可条款 | □ |
| 6 | 提交 / 认证 | 认证说明、测试账号 | □ |

---

## 2. 定价和可用性（Pricing and availability）

按产品策略填写；常见免费应用：

| 项 | 建议 |
|----|------|
| 市场 | 按目标国家勾选（可先中国 + 主要英语市场，或「全部」） |
| 价格 | 免费（若无应用内购走 Store 结算） |
| 可见性 | 公开（除非要私下链接） |
| 发布 | 通过认证后立即发布 / 或手动发布 |
| 设备系列 | Win32 主要为 Windows 桌面 |

付费或 IAP 需另配商务与税务信息，本手册按**免费桌面工具**默认。

---

## 3. 属性（Properties）

### 3.1 类别

| 字段 | 建议 | 说明 |
|------|------|------|
| 主类别 | **工作效率** | 磁贴工作台、日历、笔记等主场景 |
| 子类别 | 留空 | 「工作效率」通常无子类 |
| 次要类别（可选） | **实用程序 + 工具** | Morph、截图等工具属性 |

> 若 UI 把「实用工具」显示成主类子项，以 Partner Center 实际可选树为准；原则是主场景偏生产力。

### 3.2 隐私策略（必填）

二选一：

1. **提供隐私策略文本** → 粘贴 [`privacy-policy.zh-CN.md`](./privacy-policy.zh-CN.md) 全文（可去 Markdown `#` 标题符）
2. **隐私策略 URL** → 托管后的 HTTPS 链接（有域名后更稳）

粘贴前替换文末联系邮箱。微软示例隐私声明仅作结构参考，**不覆盖**你的应用。

### 3.3 支持信息

| 字段 | 必填 | 建议 |
|------|------|------|
| 网站 | 推荐 | 产品官网（不要填 Store 商品页） |
| 支持联系人 | 推荐 | 支持邮箱或支持页 URL |
| 电话 / 地址 | 视情况 | 个人开发者通常可选；企业且上架法国等市场可能必填 |

### 3.4 产品声明

| 声明 | 勾选 | 说明 |
|------|------|------|
| 依赖非 Microsoft 驱动程序或 NT 服务 | 否 | 常规 Tauri 客户端 |
| 已按辅助功能指南测试 | 否 | 未专门测过不要勾 |
| 支持笔和墨迹输入 | 否 | 除非明确支持 Windows Ink |
| **包含生成式 AI 功能** | **是** | 有智能对话 / 生成内容则必选 |

### 3.5 系统要求（可选）

可不填。若填，建议只标「推荐」：

| 硬件 | 建议 |
|------|------|
| 键盘、鼠标 | 推荐 |
| 触摸屏 | 可选推荐 |
| 相机 / 麦克风 / NFC / 蓝牙 / 电话 | 不要标为必需 |
| 内存 | 可写推荐（如 4 GB），按实测 |

标成「必需」会在缺硬件设备上出现 Store 警告，并影响评价资格。

「其他系统要求」文案示例（一览页也可写）：

```text
Windows 10 版本 1809 或更高版本（推荐 Windows 11）
x64 处理器
建议 4 GB 及以上内存
首次安装需足够磁盘空间（安装包体积较大，因内嵌 WebView2 离线组件）
部分功能（登录、同步、智能对话、检查更新）需要网络连接
```

### 3.6 认证说明

完整稿见 [`认证说明.md`](./认证说明.md)。提交前确认测试账号可用。

---

## 4. 年龄分级（Age ratings）

按问卷如实作答即可。本产品为一般生产力工具，通常评级较低。注意：

- 不涉及赌博、大量暴力或成人内容
- 有用户生成内容 / 联网对话时，按问卷选项如实勾选
- 生成式 AI：按问卷中与 AI / 在线通信相关的题目如实回答

完成后保存系统生成的评级。

---

## 5. 包（Packages）

细节与校验见 [`store-package-handoff.md`](./store-package-handoff.md)。

### 5.1 填写值（当前 1.3.0）

| 字段 | 值 |
|------|-----|
| 包 URL（推荐） | `https://i-thinking-downloads.layenbrank.workers.dev/1.3.0/i-thinking_1.3.0_x64-setup.exe` |
| 包 URL（备用） | `https://pub-7a57d3acde3e4d7b9c42f56423f83c93.r2.dev/1.3.0/i-thinking_1.3.0_x64-setup.exe` |
| 体系结构 | `x64` |
| 应用类型 | `EXE` |
| 安装程序参数 | `/S`（大写） |
| 语言 | 至少 `zh-cn`；若包含英文可加 `en-us` |

### 5.2 EXE 返回代码（非必需）

| 方案 | 填写 |
|------|------|
| 安装成功 | `0` |
| 用户已取消安装 | `1`（按当前 handoff；勿与 `2` 混用） |
| 其余 | 留空 |

文件名**不必**叫 `setup.exe`；微软示例仅为占位。

### 5.3 托管约定

```text
https://{主机}/{版本}/{文件名}.exe
# 例：…/1.3.0/i-thinking_1.3.0_x64-setup.exe
```

- 自定义域未就绪时可用 Worker / r2.dev
- 桶内对象键不要重复叠产品名路径（域名已含品牌时尤其）
- 新版本用新路径；**禁止**覆盖已提交 URL 上的文件

### 5.4 自定义域（可选，非上架硬性）

绑 R2 自定义域要求：域名已在**同一** Cloudflare 账号用 Cloudflare DNS 托管。

当前账号若「未找到域」，可选：

1. 先用 Worker / r2.dev 上架  
2. 购买或添加域名后再换自定义域，并**新提交**新包 URL  

名称示例：`downloads.你的域名.com`（不必再在路径里写一层产品名）。

---

## 6. Store 一览（Listings）— 简体中文

必填：说明、≥1 张截图、1:1 Store 徽标、适用的许可条款。

### 6.1 产品名称

使用已保留名称：**i thinking**（与安装包显示名一致）。

### 6.2 说明（必填，≤10000）

可直接粘贴：

```text
i thinking 是一款 Windows 桌面生产力工具，提供可自由排布的磁贴工作台，帮助你把导航、日程、笔记与常用工具放在同一块桌面画布上。

主要能力包括：
• 磁贴工作台：自由排布、镜像桌面、收藏与外观定制，布局默认保存在本地
• 快捷导航与书签：快速打开常用网站与入口
• 时钟、倒计时、日历事件与提醒
• Markdown / 代码等本地笔记
• 截图与标注（由你主动触发）
• Morph 文档处理：合并、提取、转换等，处理一般在本地完成
• 可选账号与云同步
• 可选智能对话（生成式 AI；不使用时不影响本地核心功能）

设计原则：本地优先、按需联网。打开应用不会自动上传你的本地内容；涉及登录、同步、智能对话、检查更新或获取网站图标时才会发起必要网络请求。

适用于希望把浏览器新标签页式的效率体验带到桌面的用户。系统要求：Windows 10/11 x64。部分功能需要网络连接。
```

### 6.3 此版本中的新增功能

- **首次提交**：留空  
- **后续更新**：写本版变更（≤1500），例如：

```text
• 版本 1.3.0：……
• 修复 ……
• 改进 ……
```

### 6.4 产品功能（可选，最多 20 条，每条 ≤200）

不要手动加项目符号（Store 会自动加）。示例：

```text
自由排布的磁贴工作台
本地保存布局与个性化设置
快捷导航与书签收藏
时钟、倒计时与日历提醒
Markdown / 代码本地笔记
截图裁剪与标注
Morph 本地文档处理
可选账号与云同步
可选智能对话（生成式 AI）
开机自启与系统托盘
中英文界面支持
```

### 6.5 简短说明（推荐，建议 ≤270 显示友好）

```text
可自由排布的桌面磁贴工作台：导航、日历、笔记、截图与本地文档处理，本地优先，按需联网。
```

### 6.6 关键字（可选，每词 ≤40，最多 7 个词条）

示例（按实际 UI 已填可微调）：

```text
磁贴工作台
桌面生产力
本地笔记
日历提醒
截图标注
文档处理
新标签页
```

注意：所有关键字合计有总词数限制（约 21 个独立词），勿堆砌无关热词。

### 6.7 版权和商标信息（可选，≤200）

**用途**：Store 页一行权属展示；**不是**完整许可协议（许可在下一栏）。

推荐粘贴：

```text
© 2026 i thinking. 保留所有权利。
```

备选：

```text
© 2026 i thinking. All rights reserved.
```

```text
© 2026 i thinking / layen. 保留所有权利。
```

配置要点：

| 要点 | 说明 |
|------|------|
| 必填？ | 否，建议填 |
| 包含 | 版权符号 © + 年份 + 权利人 +「保留所有权利」 |
| 年份 | 首次发布年或当前年（2026） |
| 商标符号 | 未注册一般不用 ®；已注册再用 |
| 勿粘贴 | 整份许可条款、隐私全文 |
| 与「开发者」字段 | 「开发者」可填 `layen`；版权行可用产品名 `i thinking` |

### 6.8 适用的许可条款（必填，≤10000）

粘贴 [`license-terms.zh-CN.md`](./license-terms.zh-CN.md) 全文，替换联系邮箱。

也可极简（不推荐，完整稿更稳）：

```text
专有软件许可（Proprietary）。© 2026 i thinking. 保留所有权利。详见产品内或发布方提供的完整许可条款与隐私声明。
```

未提供自有条款时，部分市场可能适用微软标准应用许可条款；仍建议提交自有条款。

### 6.9 开发者（Developed by）

```text
layen
```

或与对外品牌统一为 `i thinking`（与版权行一致即可，避免互相矛盾）。

### 6.10 图像资产

| 资产 | 要求 | 建议 |
|------|------|------|
| 屏幕截图 | 至少 1 张；推荐 ≥4；最多 10 | 工作台全景、日历、笔记、Morph、设置各 1 |
| Store 徽标 1:1 | **必填** | 方形盒绘 / 应用图标适配 |
| Store 海报 2:3 | 推荐 | 竖版宣传图 |
| 格式 | 常见 PNG/JPEG | 清晰、无过大压缩噪点 |
| 内容 | 真实 UI | 避免误导性假界面；勿含无关竞品名 |

截图建议分辨率：常见桌面 1920×1080 或文档推荐尺寸；保持比例一致更美观。

可参考：[屏幕截图和图像](https://learn.microsoft.com/zh-cn/windows/apps/publish/publish-your-app/msi/screenshots-and-images)、[Store 资产指南](https://learn.microsoft.com/zh-cn/windows/apps/design/downloads/#microsoft-store-asset-guidance-kit)。

### 6.11 多语言

至少完成 **简体中文**。若包支持英文，建议补一份 **English (United States)** 一览（说明 / 功能 / 许可可另备英文版；暂无中文许可时需评估目标市场要求）。

---

## 7. 提交前自检

### 包与签名

- [ ] 包 URL HTTPS 直链可下载（非 HTML / 登录页）
- [ ] `Content-Type` 合理（如 `application/octet-stream`）
- [ ] 体积 / 校验与本地一致（可用 Worker `?meta=1`）
- [ ] 已用可信 CA 代码签名
- [ ] 静默参数 `/S` 本地验证通过
- [ ] `webviewInstallMode` 为 `offlineInstaller`
- [ ] 提交后不会覆盖该版本 URL 文件

### 属性与合规

- [ ] 隐私策略已填且邮箱真实
- [ ] 已勾选「包含生成式 AI」（若适用）
- [ ] 认证说明含测试账号；账号可登录
- [ ] 支持联系方式有效

### 一览

- [ ] 说明、截图、1:1 徽标、许可条款已齐
- [ ] 版权和商标信息已填一行
- [ ] 无其他产品名称、无夸大承诺
- [ ] 关键字与功能列表无堆砌无关词

### 法律文稿占位

- [ ] 隐私声明、许可条款中的 `【请填写您的联系邮箱】` 已替换

---

## 8. 发版后更新流程

1. 构建并签名新安装包  
2. 上传到**新路径**（如 `/1.3.1/...`）  
3. Partner Center → 该应用 → **更新提交**  
4. 包页改新 URL；一览填写「此版本中的新增功能」  
5. 若应用内 updater 仍指向旧托管，同步改 `latest.json` 等端点（关 GitHub 前必须迁走）  
6. 提交认证  

---

## 9. 可粘贴速查（中文一览）

**版权和商标信息**

```text
© 2026 i thinking. 保留所有权利。
```

**开发者**

```text
layen
```

**简短说明**

```text
可自由排布的桌面磁贴工作台：导航、日历、笔记、截图与本地文档处理，本地优先，按需联网。
```

**安装程序参数**

```text
/S
```

**安装成功返回码**

```text
0
```

完整说明、许可、隐私、认证说明见上文各节与对应 markdown 文件。

---

## 10. 常见问题

**Q：文件名必须是 setup.exe 吗？**  
A：否。只要是 `.exe`/`.msi` 的版本化 HTTPS 直链即可。

**Q：必须买域名吗？**  
A：否。Worker / r2.dev 可用于上架；自定义域仅影响品牌与长期运维。

**Q：为什么绑不下 downloads.i-thinking.com？**  
A：该根域须已在本 Cloudflare 账号用 Cloudflare DNS 托管；账号「域名」为空时无法绑定。

**Q：MSIX 在哪配置？**  
A：当前是 Win32 EXE 路径，无 MSIX 上传项。改走 MSIX 需另建产品类型并自行打包，Tauri 默认不产出 MSIX。

**Q：许可条款和版权信息有何区别？**  
A：版权是一行权属声明；许可条款是完整使用协议（一览必填）。
