import { clsx } from 'clsx'

import styles from '@/views/overview/components/summary.module.scss'

function Summary() {
  return <footer className={clsx(styles.summary)}></footer>
}

export default Summary
