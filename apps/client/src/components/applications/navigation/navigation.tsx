import styles from '@/components/applications/navigation/navigation.module.scss'

export default function Navigation(props: AppComponentProps) {
	return <div className={styles.navigation}>navigation</div>
}
