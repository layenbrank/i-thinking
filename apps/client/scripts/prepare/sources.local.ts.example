/**
 * 本机源链覆盖（勿提交）。复制为 sources.local.ts 后按需调整顺序。
 *
 *   cp sources.local.ts.example sources.local.ts
 *
 * 未写的字段沿用 sources.ts；写了 `sources` 则整表替换该产物源链。
 */
import { SOURCES, type SourcesOverride } from './sources.ts'

const SOURCES_LOCAL = {
  corex: {
    sources: [
      {
        type: 'local',
        id: 'cargo',
        dir: '${CARGO_TARGET_DIR}/${HOST_TRIPLE}/release'
      },
      // { type: 'mirror', id: 'ghproxy', base: 'https://ghproxy.com/' },
      ...SOURCES.corex.sources
    ]
  },
  pandoc: {
    sources: [
      // { type: 'mirror', id: 'ghproxy', base: 'https://ghproxy.com/' },
      ...SOURCES.pandoc.sources
    ]
  },
  ffmpeg: {
    sources: [
      // { type: 'mirror', id: 'ghproxy', base: 'https://ghproxy.com/' },
      ...SOURCES.ffmpeg.sources
    ]
  }
} satisfies SourcesOverride

export default SOURCES_LOCAL
