pub mod ui {
    pub mod contextmenu;
    pub mod tray;
}

pub mod utils {
    // pub mod bookmark;
    pub mod bootstrap;
    // pub mod client;
    // pub mod installer;
    pub mod invoke;
    pub mod scan;
}
