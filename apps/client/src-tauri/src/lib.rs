pub mod ui {
    pub mod contextmenu;
    pub mod tray;
}

pub mod utils {
    pub mod bootstrap;
    pub mod invoke;
    pub mod scan;
    // pub mod bookmark;
    // pub mod client;
    // pub mod installer;
}
